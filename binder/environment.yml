name: ldaca-binder
channels:
  - conda-forge
dependencies:
  - python=3.14
  - pip
  - pip:
      - uv
      - jupyter_server_proxy
      - ldaca-wordflow==0.4.2
      - --extra-index-url https://download.pytorch.org/whl/cpu
      - torch
